<%= form_with(model: dog_ownership) do |form| %>
  <% if dog_ownership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dog_ownership.errors.count, "error") %> prohibited this dog_ownership from being saved:</h2>

      <ul>
        <% dog_ownership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :dog_id, style: "display: block" %>
    <%= form.text_field :dog_id %>
  </div>

  <div>
    <%= form.label :owner_id, style: "display: block" %>
    <%= form.text_field :owner_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
